name: Ping Streamlit App

on:
  schedule:
    - cron: "*/30 * * * *" # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          echo "🌐 Pinging Streamlit app..."
          curl -I https://sb-client-lead.streamlit.app/
