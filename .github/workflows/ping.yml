name: Ping Streamlit App

on:
  schedule:
    - cron: "*/30 * * * *" # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          echo "🌐 Pinging Streamlit app..."
          curl -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" -L https://sb-client-lead.streamlit.app/
