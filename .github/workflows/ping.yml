name: Ping Streamlit App

on:
  schedule:
    - cron: "*/10 * * * *" # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          echo "🌐 Pinging Streamlit app..."
          curl -s -o /dev/null -w "%{http_code}\n" https://sb-client-lead.streamlit.app/
